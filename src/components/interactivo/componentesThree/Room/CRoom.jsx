import React from 'react'
import Room from './Room'

export const CRoom = ({position , markers2 }) => {

    return (
            <Room
                position = { position }
                markers2 = { markers2 }/>
        
    )
}
